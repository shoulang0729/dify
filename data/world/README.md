# data/world — 架空世界マスタ

台本（`mock/js/data/scenarios/**`）・KB 用ダミー文書（`dify/kb/**`）・テスト入力（`dify/tests/**`）・
ユースケース文書（`docs/dify/usecases/*.md`）に共通して登場する、架空の会社・人・品番・設備・数字の**正本**。

出典：`docs/handoff/2026-09-07-repo-layout-v2.md` §2（設計）。Issue #84 PR-1 で新設。
金融（在中日系銀行）の第 2 世界は `docs/handoff/2026-09-08-finance-catalog.md`（Issue #120 PR-2）で追加。

**この会社・人物・数値はすべて架空です。実在の企業・製品とは関係ありません。**

## 2 つの世界（製造業／金融）

- **製造業**：`data/world/mfg/`（下表）。青嶺精工／蘇州工場の世界
- **金融**：`data/world/fin/`（下表）。碧洋銀行／上海本部の世界
- **2 つの世界の語彙は混ぜない**。金融の台本・KB・テストは `data/world/fin/` にある値だけを使い、製造業マスタの語（青嶺精工・K 社・PX-200 など）を流用しない。逆も同様
- 製造業側は初版（Issue #84 PR-1）でこのディレクトリ直下に置かれていたが、Issue #120 PR-2 で `data/world/mfg/` へ移動した（`git mv`。内容は不変）。**参照するときは新しいパス `data/world/mfg/*` を使うこと**

## 何がここにあるか（製造業／`data/world/mfg/`）

| ファイル | 内容 |
|---|---|
| `company.md` | 社名（ja/zh/en）・現地法人・拠点・事業内容 |
| `org.csv` | 部署（`SCENARIOS[].persona.role` から抽出） |
| `people.csv` | 人物 17 名（`SCENARIOS[].persona` ＋ `FEED.persona`）。主務 `title_*` に加え兼務 `alt_title_ja/zh/en`（`;` 区切り、3 列とも同じ順序）を持つ（Issue #153 PR-3 で追加） |
| `products.csv` | 品番（台本の `SK-…`／`ASSY-…` など） |
| `equipment.csv` | 設備・金型・治具（`PX-200`・`D-118` など） |
| `partners.csv` | 取引先記号（`K 社` など。**社名は付けない**）とパートナー連携 6 サービスの役割名、および GN-07（幹部来訪・出張アテンド）が使う旅行会社記号 `R 社` |
| `kpi.csv` | 不良率・稼働率・生産数など（基準値。改善事例など別文脈の値は note で区別） |
| `calendar.md` | 会計年度・文書番号の体系・世界の「今日」 |
| `documents.csv` | 規程・マニュアル・社内システムのレコード ID の台帳（`kind`＝`rule`/`record_id`。発番されない ID。Issue #153 PR-2 で新設） |
| `hotels.csv` | 会社契約ホテル（GN-07。3 言語の名称・住所） |
| `vehicles.csv` | 社用車・旅行会社手配のハイヤー（GN-07） |
| `airports.csv` | 空港（GN-07。IATA コードは実在。§4-5） |
| `routes.csv` | 区間所要時間（GN-07。空港・ホテル・拠点を横断で参照） |
| `contacts.csv` | 緊急連絡先（GN-07。電話番号はすべて架空） |

## 何がここにあるか（金融／`data/world/fin/`）

| ファイル | 内容 |
|---|---|
| `company.md` | 銀行名（ja/zh/en）・現地法人・拠点（上海本部・大連支店・日本本店）・事業内容 |
| `org.csv` | 部署 10（営業第一部・営業第二部・審査部・リスク統括部・コンプライアンス部・経営企画部・事務統括部・市場業務部・財務部・システム部） |
| `people.csv` | 人物 14 名（日本人駐在 5・現地採用 9）。製造業マスタ 17 名と姓名の重複なし。`alt_title_ja/zh/en` 列は製造業マスタと列構成を揃えるために追加（全行空。Issue #153 PR-3） |
| `clients.csv` | 顧客企業の記号 5（甲社〜戊社。**社名は付けない**。製造業の `partners.csv` とは別体系） |
| `vendors.csv` | 仕入先系の記号（GN-07 が使う旅行会社記号 `己社`。`clients.csv`〔顧客専用〕とは別体系） |
| `products.csv` | 取扱商品・サービス 8（貸出・外為・貿易金融・キャッシュマネジメント・投信販売） |
| `kpi.csv` | 貸出残高・預金残高・与信先数・延滞率・手数料収益・稟議処理日数・照会一次回答率・当局報告件数（基準値／目標／前月） |
| `calendar.md` | 会計年度（4/1〜3/31）・月次クローズ・当局報告の期日・決算発表シーズン・世界の「今日」（2026-09-08）・文書番号体系 |
| `documents.csv` | システムのエラーコードの台帳（`kind`＝`code`。Issue #153 PR-2 で新設） |
| `hotels.csv` | 会社契約ホテル（GN-07。3 言語の名称・住所） |
| `vehicles.csv` | 公務車・旅行会社手配のハイヤー（GN-07） |
| `airports.csv` | 空港（GN-07。IATA コードは実在。§4-5） |
| `routes.csv` | 区間所要時間（GN-07。空港・ホテル・拠点を横断で参照） |
| `contacts.csv` | 緊急連絡先（GN-07。電話番号はすべて架空） |

金融には `equipment.csv` を置かない（設備が主役でない業種のため。設計書 §7-2）。人物・KPI の実データは
台本（PR-4）でまだ参照されていない（`kpi.csv`/`people.csv` の `note`/`source` 列に明記）。

**申し送り（解消済み・Issue #120）**：`fin/org.csv` の 10 部署のうち担当者が未配置だった営業第二部・
リスク統括部・財務部・システム部には、それぞれ 1 名（林静・中野隆・鄭麗華・徐涛）を配置した。`company.md` が
挙げる大連支店・日本本店にも紐づく人物（潘婷＝大連支店長・石田由美＝経営企画部 日本本店担当）を追加した。
台本（`mock/js/data/scenarios/**`）への反映は別 PR（人を足しただけで台本の振り直しはしていない）。

## 重要な原則（load-bearing。`docs/handoff/2026-09-07-repo-layout-v2.md` §2-1）

**`data/world/` は正本だが、自動反映はしない。** モックのデータ層も KB 文書も、マスタから生成しない
（台本は文脈で表現が変わる：目標値・前月値・当月値）。`tools/check-world.mjs` は**報告するだけ**（FAIL にしない）。

## 追加時のルール

1. **新しい人名・社名・品番・設備・数字を出すときは、まず `data/world/` に足す。** 台本・KB 文書・テストはマスタにある値だけを使う
2. 既にある値を**別の文脈で違う数字にしたい**とき（改善事例など）は、`kpi.csv` に行を足して `note` で文脈を書く。台本側に裸の新しい数字を書かない
3. **取引先に社名を付けない**（PM 決定 PT-8）。記号（`K 社`）と役割名だけ。空白は `K 社`（半角スペース）に揃える — 既存の `K社` 8 か所は本 PR では**直さない**（`mock/**` 不可侵）。`tools/check-world.mjs` が warn として出し続ける
4. 実在の企業名・型番・URL・人名を混ぜない（`docs/dify/implementation-guide.md` §5-3）
5. 個人情報の見本（身分証 18 桁・電話 11 桁）は**架空値**を `people.csv` に持たず、テスト側（`dify/tests`）にだけ置く

## 未統一（`node tools/check-world.mjs` の warn と 1 対 1。Issue #153 で仕分けを見直した）

`data/world/` を正本と決めても、台本・KB・実装リファレンス側の食い違いを一度に全部揃えるわけではない。
ここに載っているのは**現時点で `node tools/check-world.mjs` が報告する warn** で、行数は warn 件数と一致する。
解消の仕方は行ごとに違う：①のように**正本へ追加で決着**したものもあれば、②のように**別 PR で解消予定**、
④⑤⑥⑦のように**`mock/**` を触るため本リポジトリの正本作業では触らない**ものもある
（Issue #153 の全体設計は `docs/handoff/2026-09-08-world-consistency-cleanup.md`）。

Issue #153 の経緯：文書番号 54 種・役職ゆれ 6 名が正本未登録だったのを PR-1（集計キャップと判定順序の是正）・
PR-2（書式 17 種の追加＋規程・社内 ID 台帳 `documents.csv` の新設）・PR-3（本節。役職ゆれ 6 名を
`people.csv` の `alt_title_ja/zh/en` に登録し、`check-world.mjs` の W2 を「正本に無い役職を検出する」形に
作り替えた）・PR-4（実装リファレンスの二重採番是正）・PR-5（KB の様式番号衝突 `QC-27-D`→`QC-42` の解消）
で順に片付けている。

### 製造業（mfg）— 10 件

| # | 食い違い | マスタ側の扱い | 出典 |
|---|---|---|---|
| ① | 社名の表記回数：`青嶺精工`(ja) 3 回・`青岭精工`(zh) 14 回・英名 `Seirei Seiko` 0 回 | `company.md` で ja/zh/en の 3 言語を定義済み。英語台本を書く別 PR で解消 | 台本全体 |
| ② | 取引先記号の空白ゆれ：`K社`（半角スペースなし）8 件。正は `K 社`（`partners.csv`） | `mock/**` を触るため本リポジトリの正本作業では対象外（設計書 §8） | 台本全体 |
| ③ | 品番未登録：`SK-2207` | `products.csv` の枝番体系（`-A`/`-B`）をどう持つかの設計判断が必要。別 Issue | `SCENARIOS.mfg[].script` |
| ④ | 品番未登録：`SK-2207-A` | 同上 | 同上 |
| ⑤ | 設備未登録：`J-2207` | 同上 | 同上 |
| ⑥ | 設備未登録：`PX-350` | 同上 | 同上 |
| ⑦ | 設備未登録：`PX-100` | 同上 | 同上 |
| ⑧ | KPI の同名別値：`defect_rate`（不良率）は月報 0.42%・第36週報のライン別内訳（L1 0.31%／L2 0.44%／L3 0.52%）・別週報（DC-08）0.62%・相関分析（nm5）の 0.5%・改善事例 3.8%→1.5% の複数系統がある | `kpi.csv` の `defect_rate` 行の `note` で文脈ごとに整理済み。検査側を「文脈付きの値を許す」形にするかは別判断 | `dc.js`／`nm.js`／`dify/samples/NM-03-S03` |
| ⑨ | KPI の同名別値：`uptime`（稼働率）は月報 87.2%・第36週報のライン別内訳（L1 89.1%／L2 86.4%／L3 84.0%）の系統がある（合計 86.5% は前月値と一致） | `kpi.csv` の `uptime` 行の `note` で文脈ごとに整理済み。W8 強化（`check-world.mjs` の表記ゆれ・隣接吸収）で新たに見えるようになった | `nm.js`／`dify/samples/NM-03-S03` |
| ⑩ | KPI の同名別値：`output`（生産数）は月報 186,400 個・第36週報のライン別内訳（L1〜L3・合計 36,600 個）・日報 6,200 個・翌月展望 192,000 個の複数系統がある | `kpi.csv` の `output` 行の `note` で文脈ごとに整理済み。同上（W8 強化で新たに見えるようになった） | `dc.js`／`nm.js`／`dify/samples/NM-03-S01`〜`S03` |

### 金融（fin）— 1 件

| # | 食い違い | マスタ側の扱い | 出典 |
|---|---|---|---|
| ⑪ | 社名の表記回数：`碧洋銀行`(ja) 6 回・`碧洋银行`(zh) 2 回・英名 `Hekiyo Bank` 1 回のみ | `company.md` で ja/zh/en の 3 言語を定義済み。英語台本を書く別 PR で解消 | 台本全体 |

**解消済み（Issue #205 PR-4）**：カバレッジ「`people.csv` にいるがどこにも出てこない 6 名（林 静・中野 隆・
鄭 麗華・徐 涛・潘 婷・石田 由美）」は、`dify/samples/{KN-06,KN-08,DC-09,RS-01,RS-03}/` の入力サンプル
（15 本）に 6 名全員をペルソナとして登場させたことで解消した（`node tools/check-world.mjs` の `[fin] W9`
が warn 0 に）。

## `tools/check-world.mjs` の読み方

```
node tools/check-world.mjs            # 報告のみ。常に exit 0
node tools/check-world.mjs --strict   # 1 件でも不一致なら exit 1（食い違いを潰す PR で使う）
node tools/check-world.mjs --all      # W6/W7 のような件数が多い検査も全件表示する
npm run world                          # = node tools/check-world.mjs
```

出力は業種（`mfg`／`fin`）ごとに W1〜W9 の検査を行い、件数と代表例を出す（詳細は `tools/check-world.mjs`
の冒頭コメント）。**CI には入れない**（`npm test` に足さない）。現時点（Issue #205 PR-4 で金融 W9 の
6 名カバレッジ warn を解消した後）で製造業側は上の「未統一」表に対応する warn（10 件）、金融側は 1 件が
必ず出る（合計 11 件）。W8（KPI）は表記ゆれ（簡体字・繁体字・異体字。例：稼働率／稼動率／稼动率）と、ラベルと
数値の間にコロン・括弧・助詞が挟まる「隣接の崩し」を両方吸収したうえで `data/world/*/kpi.csv` の
全指標（ハードコードしない）を対象にする。⑧〜⑩ は、この強化で新たに見えるようになった「月次基準値と
週報・日報・翌月展望など別系統の値との食い違い」で、`kpi.csv` の `note` 列に文脈を書いたうえで
恒久的に warn させ続ける方針（`check-world.mjs` を「文脈付きの値を許す」形にはしていない）。

### W8（KPI）が拾える範囲と拾えない範囲（重要）

W8 は 2026-09-10 までに**表記の書き換えで 2 回すり抜けられた**（簡体字への書き換え・括弧で隣接を
崩す）。その 2 件を含む 3 パターンは強化（上記 PR）で拾えるようになったが、**さらに reviewer が独立に
試したところ、強化後もなお次の 6 パターンは拾えない**ことを一時ファイルでの実測で確認した（実装は
このまま変えていない。塞がない理由は後述）。詳細な実装は `tools/check-world.mjs` の
`buildKpiValueRegex` 付近のコメントを参照。

**拾う**：
- `kpi.csv` の全行（`name_ja`/`name_zh`。数語だけのハードコードではない）のラベルと登録値
  （`target`/`current`/`prev`）の突き合わせ
- ラベルの表記ゆれ（働／動／动）
- ラベルと数値の間、最大 6 文字までの隣接の崩し（括弧・助詞「は/の/が」・コロン・読点など。
  この 6 文字の中に実際の改行文字が入っている場合も含む）

**拾わない**（reviewer 実測。6 パターン）：
1. 全角数字＋全角％（例: `稼働率８６．０％`）
2. 改行を挟む（例: `稼働率\n86.0%`）——ただし挟まるのが本物の改行文字なら上記のとおり拾える。
   拾えないのは `dify/tests/**.json` のように**生テキストとして読み JSON.parse しない**対象で、
   改行がエスケープ表現（バックスラッシュ＋`n` の 2 文字）のまま残っているケースに限る
3. 小数表記（例: `稼働率 0.86`）
4. 「パーセント」表記（例: `稼働率 86 パーセント`）
5. ラベルの言い換え（例: `設備の稼働は 86.0%`）
6. 隣接 7 文字以上、上限超え（例: `稼働率-------86.0%`）

**なぜ全部塞がないか**：`check-world.mjs` は **warn のみで CI に入れない道具**。誤検知（無関係な数字を
拾ってしまうこと）を増やすと、この「未統一」台帳自体の価値が落ちる。上記 6 つを塞がないのは
**設計判断であって、やり残しではない**。

**⚠️ いちばん大事なこと**：**W8（延いては `check-world.mjs` 全体）を通ることは、`CLAUDE.md` §2-13
「架空データの正本は `data/world/`」を守っている証明にはならない。** 新しい数値を書くときの本来の
手順は `data/world/` を見て書くことであり、この検査は取りこぼしを拾うための保険にすぎない。
