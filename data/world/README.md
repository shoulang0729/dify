# data/world — 架空世界マスタ

台本（`mock/js/data/scenarios/**`）・KB 用ダミー文書（`dify/kb/**`）・テスト入力（`dify/tests/**`）・
ユースケース文書（`docs/dify/usecases/*.md`）に共通して登場する、架空の会社・人・品番・設備・数字の**正本**。

出典：`docs/handoff/2026-09-07-repo-layout-v2.md` §2（設計）。Issue #84 PR-1 で新設。

**この会社・人物・数値はすべて架空です。実在の企業・製品とは関係ありません。**

## 何がここにあるか

| ファイル | 内容 |
|---|---|
| `company.md` | 社名（ja/zh/en）・現地法人・拠点・事業内容 |
| `org.csv` | 部署（`SCENARIOS[].persona.role` から抽出） |
| `people.csv` | 人物 17 名（`SCENARIOS[].persona` ＋ `FEED.persona`） |
| `products.csv` | 品番（台本の `SK-…`／`ASSY-…` など） |
| `equipment.csv` | 設備・金型・治具（`PX-200`・`D-118` など） |
| `partners.csv` | 取引先記号（`K 社` など。**社名は付けない**）とパートナー連携 6 サービスの役割名 |
| `kpi.csv` | 不良率・稼働率・生産数など（基準値。改善事例など別文脈の値は note で区別） |
| `calendar.md` | 会計年度・文書番号の体系・世界の「今日」 |

## 重要な原則（load-bearing。`docs/handoff/2026-09-07-repo-layout-v2.md` §2-1）

**`data/world/` は正本だが、自動反映はしない。** モックのデータ層も KB 文書も、マスタから生成しない
（台本は文脈で表現が変わる：目標値・前月値・当月値）。`tools/check-world.mjs` は**報告するだけ**（FAIL にしない）。

## 追加時のルール

1. **新しい人名・社名・品番・設備・数字を出すときは、まず `data/world/` に足す。** 台本・KB 文書・テストはマスタにある値だけを使う
2. 既にある値を**別の文脈で違う数字にしたい**とき（改善事例など）は、`kpi.csv` に行を足して `note` で文脈を書く。台本側に裸の新しい数字を書かない
3. **取引先に社名を付けない**（PM 決定 PT-8）。記号（`K 社`）と役割名だけ。空白は `K 社`（半角スペース）に揃える — 既存の `K社` 8 か所は本 PR では**直さない**（`mock/**` 不可侵）。`tools/check-world.mjs` が warn として出し続ける
4. 実在の企業名・型番・URL・人名を混ぜない（`docs/dify/implementation-guide.md` §5-3）
5. 個人情報の見本（身分証 18 桁・電話 11 桁）は**架空値**を `people.csv` に持たず、テスト側（`dify/tests`）にだけ置く

## 未統一（PR-2 以降で揃える。今は「マスタ側に正を1つ書き、ゆれの一覧をここに残す」だけ）

初版は既存資産から抽出した値だけを載せ、新規に発明していない。以下は台本・文書間ですでに食い違っている点。
`tools/check-world.mjs` の報告（W1〜W9）で継続的に検出される。

| # | 食い違い | マスタ側の扱い | 出典 |
|---|---|---|---|
| 1 | 役職ゆれ 5 名（王 磊・周 敏・孫 麗・呉 婷・銭 芳） | `people.csv` は「主務＋兼務・別業務メモ」で 1 行にまとめた（`note` 列） | `SCENARIOS[].persona`（43 ブロック） |
| 2 | 社名の表記回数：`青嶺精工`(ja) 2 回・`青岭精工`(zh) 14 回・英名 0 回 | `company.md` で ja/zh/en の 3 言語を定義 | 台本全体 |
| 3 | 取引先記号の空白ゆれ：`K 社`（半角スペースあり、133 回）と `K社`（なし、8 回） | `partners.csv` は `K 社`（半角スペースあり）を正とする | 台本全体 |
| 4 | `DC-02` の紛らわしさ：設計書 §2-3 は「会議室コードと管理番号（DC-02 議事録作成）が衝突する」と指摘している。本 PR の grep では会議室そのものに `DC-02` という文字列は見つからなかったが（`gn.js` の会議室表記は「2 階 B 会議室」）、今後 `DC-02` を場所コードとして使わないよう記録として残す | — | 設計書 §2-3 |
| 5 | 設備コード `PX-200` の用途不一致：`kn.js`/`nm.js` は「プレス機」（アラーム E-47）、`dify/kb/KN-01/工程条件書_旋盤ライン3.md` は「旋盤ライン 3 の NC 旋盤（PX-200×2）」 | `equipment.csv` の `PX-200` 行の `note` に両方の記載を併記 | `kn.js` L72 ほか／`dify/kb/KN-01/工程条件書_旋盤ライン3.md` |
| 6 | 品番の枝番表記ゆれ：`SK-3318` と `SK-3318-A`（本文はメールの誤記として扱っている） | `products.csv` の `SK-3318` 行の `note` に記載 | `gn.js` |
| 7 | 世界の「今日」のずれ：大半の台本は 2025-09 前後が現在だが、`pt.js`（パートナー連携）の一部は本文中に 2026 年の日付を明示している | `calendar.md` §世界の「今日」に両方を記載 | `pt.js` |
| 8 | 文書番号の年桁混在：`RFQ-2025-118`（4 桁）と `RFQ-26-031`（2 桁） | `calendar.md` の体系表に両方を正として明記 | `gn.js`／`nm.js` |
| 9 | KPI の同名別値：`defect_rate`（不良率）は月報 0.42%・別週報 0.62%・別サービスの改善事例 3.8%→1.5% の 3 系統がある | `kpi.csv` の `defect_rate` 行の `note` に文脈ごとに整理 | `dc.js`／`nm.js` |

## `tools/check-world.mjs` の読み方

```
node tools/check-world.mjs            # 報告のみ。常に exit 0
node tools/check-world.mjs --strict   # 1 件でも不一致なら exit 1（食い違いを潰す PR で使う）
npm run world                          # = node tools/check-world.mjs
```

出力は W1〜W9 の検査ごとに件数と代表例を出す（詳細は `tools/check-world.mjs` の冒頭コメント）。
**CI には入れない**（`npm test` に足さない）。初版では上の「未統一」表に対応する warn が必ず出る。
