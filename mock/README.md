# 🧪 AIエージェント 画面案モック ／ AI智能体界面方案（演示）

> ⚠️ **これは本番システムではありません。社内検討用のコンセプト確認モックです。**
> チャットの応答はダミー（モック応答）で、実際のLLM処理・データ連携は行っていません。

2種類のモックを見比べるための一式です。`index.html` を開くと比較ランディングから各モックへ移動できます。

## 収録モック

| | 画面 | 内容 |
|---|------|------|
| **A** | `catalog.html` — AIエージェントカタログ | 左ナビ3案を切替（A アコーディオン / B カテゴリレール / C 全展開ツリー）。大分類6×中分類12×33サービス。一覧 → 詳細 → チャット起動。 |
| **B** | `top.html` — トップページ（ポータル） | 左サイドバー固定のホーム。レイアウト3案を切替（ダッシュボード / バナー＋カテゴリ / 業務フィード）。カテゴリ一覧 → 個別の業務画面（チャット）まで遷移。 |

いずれも日本語・中国語併記、純粋な静的 HTML/CSS/JS（ビルド不要）。

## ディレクトリ構成

```
mock/
├── index.html      比較ランディング（A / B へのリンク）
├── catalog.html    モックA：AIエージェントカタログ（自己完結）
├── top.html        モックB：トップページ（自己完結）
├── .nojekyll       GitHub Pages の Jekyll 処理を無効化
└── README.md       このファイル
```

> デザイントークンは各 HTML に内包済み。外部の CSS / 画像ファイルへの依存はありません。

## ローカルで確認

`index.html`（または各 HTML）をブラウザで直接開くだけで動作します。サーバー不要。

## GitHub Pages で公開（共有用）

1. この `mock/` フォルダをリポジトリに配置
2. リポジトリ **Settings → Pages**
3. **Source: Deploy from a branch** → Branch `main` / フォルダ `/(root)`
4. 数分後、`https://<org>.github.io/<repo>/mock/` で公開

> Private リポジトリで Pages を公開するには GitHub Pro / Team / Enterprise が必要です。

## 位置づけ

| 項目 | 内容 |
|------|------|
| 種別 | コンセプト確認用モック（非本番） |
| 用途 | 社内レビュー・UI/UX方向性の検討 |
| データ | すべてダミー（サンプルのユースケース） |
| チャット | モック応答（LLM未接続） |
