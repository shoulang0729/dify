# docs/artifacts/ — 画面案（claude.ai / Cowork の HTML アーティファクト）の保管場所

`<slug>/index.html` として置く。Pages では公開されない（`pages.yml` は `path: mock`）。
出所（アーティファクト URL）と取得日、対象画面・変更点は、対応する `docs/handoff/cowork/` の手渡しメモの「画面案」欄に書く。
ここにあるものは**設計の入力**であり実装ではない。実装は architect の設計書を経て implementer が行う。
