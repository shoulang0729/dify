# PM 決定記録（2026-09-06）— 顧客版カタログ A / デモ遷移 B に対する決定

architect の設計書（`2026-09-06-customer-catalog-data.md` / `2026-09-06-demo-scenarios.md`）は変更しない。
本ファイルは **PM の決定**を記録し、implementer / reviewer は設計書と併せて読む。設計書と食い違う点は**本ファイルが優先**。

## 0. 前提の再確認（スコープ）
- 作るのは **モック・デモ画面**。Dify の実装ではない。
- 実装フェーズに効く情報（実現性・プラグイン・参照 DSL）は `docs/dify/` に**控え**として保存済み。モックの判断を縛らない。
- 「本物っぽさ」のために突合結果から持ち帰るのは **§2 の台本修正**だけ。

## 1. 成熟度（バッジ）— 設計書 A §5-3 を上書き
| id | 設計書 | **決定** | 理由（モック上の見え方） |
|---|---|---|---|
| nm5 データ分析アシスタント | 1 | **2 試行版** | DB 接続が前提。緑で見せて後から条件を出すより黄で期待値を揃える |
| gn4 スケジュール調整 | 1 | **2 試行版** | 両拠点カレンダー接続が前提 |
| lg2 社内用語・呼称の統一 | 1 | **2 試行版** | 用語集 KB とバッチ処理が前提 |

→ 成熟度内訳：**st1 = 12 / st2 = 18 / st3 = 3**（設計書の 15/15/3 から変更）。
`counts` は不変 `{cats:7, subs:15, svcs:33, tags:35, ui:27}`。regress の差分は設計書 §5 ＋ 上記 3 件の成熟度で照合する。

## 2. 台本の文言修正（B-2 で反映。設計書 B §6 を上書き）
「AI がシステムへ勝手に書き込んだ」と読める台本は、分かっている顧客には不自然。**ドラフト／取込形式を出す**表現に直す（資料 Scope B スライド 12-13 の「判断・調査は Dify、承認・実行は BPMS」とも整合）。
| id | 修正 |
|---|---|
| kn2 設備マニュアル | Q3「同じアラームは今月何回出た？」→ 設備稼働記録も KB に入っている前提を **steps に明記**（台本は維持）|
| lg2 用語統一 | 入力の zip → **複数ファイル（file-list）** に |
| qa2 4M 影響予測 | 「ECR を起票して」→「**ECR のドラフトを作って**」 |
| lg1 翻訳 | 「用語集に登録して」→「**用語集の登録形式で出して**」 |
| gn3 受発注 | 「登録して」→「**基幹取込用 CSV で出して**」 |
zh 側も同趣旨で修正。

## 3. ペルソナと見せ方
- **デモを見せる相手＝工場長**。**エージェントを使うのは担当者**。
- 台本のペルソナは**担当者レベルのまま**（設計書 B のとおり）。
- 工場長向けの「判断・報告系を前面に」は、後続の **②ダッシュボード**パターンで拾う。B の設計は変えない。

## 4. 保留（モックに影響なし。`docs/dify/feasibility-33-services.md` §0-3 に記録済み）
F-3 データ前提の見せ方 / F-4 モデル方針 / F-5 ACL / F-6 参照資産 / F-7 実装基盤 / F-8 参照先整理 — いずれも実装フェーズで判断。

## 5. 追補
- パートナー連携ユースケース（6 者）は `2026-09-06-partner-usecases.md`（architect 作成中）で **A-2** として追加。SPEEDA は「情報ベンダ」の役割名で扱う。
- CLAUDE.md §6 バックログ「6 分類 24」→「7 分類 33」は **A-1 マージ時に PM が更新**。

## 6. パートナー連携追補（`2026-09-06-partner-usecases.md`）の PM 判断 — 就寝中のため architect 推奨で確定
| # | 論点 | 決定 |
|---|---|---|
| PT-1 | 分類 | **新設 `pt`「パートナー連携」**（既存分類＋タグ案は nm が 9 件に膨らむため不採用） |
| PT-2 | 件数 | **8**（pt1〜pt8） |
| PT-3 | 成熟度 | **st2 = pt1 pt2 pt3**（一方向の読み取り・個人情報なし）／ **st3 = pt4〜pt8**（双方向・代行・個人情報） |
| PT-4 | 新テンプレート | **増やさない**。結果パネルの「連携」行（取得元・案件番号・期限・送信していないもの）で表現 |
| PT-5 | 中分類 id | **`data` / `service`** |
| PT-6 | 分類の位置 | **末尾**（探す→品質→作る→言葉→数字→図面→汎用→パートナー） |
| PT-7 | A-2 の実施時期 | **B-1 → A-2 → B-2**（architect 推奨の A-2→B-1 から変更）。理由：B-1 の verify §9 は「台本の無い SVCS」を **warn** として扱う設計で、B-1 の台本 5 件は A-1 の id のみを参照するため FAIL しない。同一ファイルの隣接 hunk 衝突を避けるため直列にする。A-2 後の regress 期待値は `{cats:8, subs:17, svcs:41, tags:43, ui:49}` |
| PT-8 | 架空社名 | **付けない。役割名のみ**（情報ベンダ／人事情報サービス 等） |
| PT-9 | pt 台本の PR | **B-2 に合流** |
| PT-10 | PIPL 文言 | **モックの「連携」行と desc にも短く出す** |

（起床後に変更があれば、該当 PR で差し戻しではなく次の S/M レーンで対応する）

## 7. ②ダッシュボード／③業務フィード（`2026-09-06-patterns-dash-feed.md`）の PM 判断 — 2026-09-06 承認
D-1〜D-13 は **architect 推奨どおり**。D-14 のみ変更。

| # | 決定 |
|---|---|
| D-1 | ②「よく使う」＝社内全体の実績（手選びのサンプル値） |
| D-2 | 利用件数を出す＋「デモ用のサンプル値」注記 |
| D-3 | おすすめ 3 件 = dc1 / kn2 / qa3 |
| D-4 | ③ペルソナ = 李 強／製造二課 課長／蘇州工場 |
| D-5 | ヘッダー「情報システム部」は変えない |
| D-6 | ③疑似イベント 7 件 |
| D-7 | 絶対日付は出さない |
| D-8 | ②③ はサイドバーを隠す |
| D-9 | 「全件一覧」導線は置かない |
| D-10 | `regress --update` を PR-1 / PR-2 で実施。「counts 不変」は `cats/subs/svcs/tags` の 4 つ |
| D-11 | ② h1「AIエージェント ホーム」 |
| D-12 | PR-1 マージ後に Pages で ② を確認 → PR-2 |
| D-13 | 管理番号での検索はしない（要るなら別 S Issue） |
| **D-14** | **変更**：管理番号は **カード右上・詳細ヘッダー・`chat`/`demo` ヘッダー（`.chat-hdr` のサービス名の左）** の 3 か所に出す。③のフィード項目・「最近使った」には出さない。理由：デモ画面を見ながら「KN-02 の zh#2 を直して」と言えるようにするため（§2-11 の採番目的）。PR-0 の「触らない範囲」のうち `renderMain` の `chat`/`demo` 分岐は **`.code` 1 要素の挿入のみ許可** |

- 実装順：Issue #39（デモ応答テンポ）→ PR-0（管理番号）→ PR-1（②）→ PR-2（③）。すべて直列
- CLAUDE.md 更新（§2-3 `gocat`・`HOME`/`FEED`、§2-9 id 改名禁止、§6）は **PR-1 / PR-2 マージ時に PM が行う**

## 8. ② デザインパス（`2026-09-06-design-pass.md`）の PM 判断 — 2026-09-07 承認
- 背景：PR #47 の ② を PM が確認し「正しいが地味。デザイン性に欠ける」。③ は同じ部品で作るため ③ では埋まらない → ② で見え方を決めてから ③ に進む（D-12 の狙いどおり）
- 方針：**A 分類に顔をつける ＋ B ヒーローとデータ表現 ＋ C 密度と階層 を一括**（2 案比較はしない）
- §15-1〜6：architect 推奨どおり（① のカードも変わる／詳細パンくずにも色／横棒ランキング／おすすめは対等／ヒーロー帯は Smart Navy グラデ＋turquoise ライン／③ に帯は置かない）
- **§15-7：`--cat-qa` は赤ではなくアンバー**（light `#B45309` / dark `#F0A64A`）。「品質＝赤＝悪い」に見える懸念を避ける
- §15-8：`dashFreqNote` はそのまま
- 実装：`feat/42-design-pass`（PR-1.5）。PR-2 ③ は設計書 §9 の適用ルールを前提に、PR-1.5 マージ後に着手（直列）

## 9. DC-08 週報・報告のレビューと論点指摘（`2026-09-07-report-critique-service.md`）の PM 判断 — 2026-09-07
- 元ネタ：PM 執筆の Notion「報告ツッコミエージェント 指示書」「週報改善レビューAI：Dify実装指示書」。製造業の数字（不良率・稼働率・納期遵守・改善件数・コスト削減額・工数）に置き換え、課長→工場長の週報を対象にする
- 配置：`dc8` = `DC-08`、文書・資料作成／報告・会議、試行版、upload 型。データ層の追加のみ（UI 無変更）。Issue #63
- D-1〜D-6 は architect 推奨どおり：**D-1 指摘・回答台帳は新 `PC-17`**（PC-01 フィードストアとは別。期限つき指摘のみ PC-01 に `due` で併載）／D-2 DC-01 月次報告ドラフトを DC-08 に通すのは第 2 段階（モックに導線は作らない）／D-3 提出経路は Excel/Word アップロードで開始／D-4 台帳は Outline に置かない（Outline は改善後の報告案の下書きのみ）／D-5 月次品質レビューは工場長室のみ・**個人評価に使わない**を運用ルールと UI 注記の両方に／D-6 名称は「レビューと論点指摘」（zh「评审与要点追问」。「ツッコミ」「指摘」は語感が強いため画面には出さない）
- **2026-09-07 改訂（`2026-09-07-report-review-modes.md`）**：読み手向け／書き手向けで分けず、**報告種別 × 書き手→読み手 × 目的（提出前セルフチェック／受領後の論点整理）のレビューモード**を 1 サービス内で切り替える。試行版 5 モード（担当週報／組織長週報／組織長月次報告／人事・要員報告／システムトラブル報告）＋構想 10。観点は共通＋種別別（一般レベル、顧客がカスタム）。デモは `form` 型でモード選択を表す。R-1〜R-6 は architect 推奨どおり（5 モード以外は構想・`st` 2 のまま／モードマスタは Outline＋同期 JSON／**提出前モードの結果は台帳に残さない**〔D-5〕／PC-17 に `kind` 追加／導入は提出前から・デモは受領後）。Notion 2 本はこの 1 サービスに統合

## 10. GN-06 頼まれ事・放置業務の追跡（`2026-09-07-small-task-tracker.md`）の PM 判断 — 2026-09-07
- 要望：定型業務／ふと頼まれた思いつき業務／優先度が低く放置されがちな業務を、催促なしでも見過ごさず「チリツモ」で管理。一般的な管理手法で。振られた側の期限・件数、振った側の進捗、四半期棚卸しで業績に反映できる可視化。**振られた側が自分から上長へ相談できるオプション**（当初 DC-08 に付ける指示と誤解したが GN-06 が正）
- 配置：`gn6` = `GN-06`、汎用業務支援／日常業務、試行版、`form` 型。データ層の追加のみ。PC-01 フィードストアの主要な書き手（③業務フィードの出口）
- 管理手法：GTD の捕捉／2 分ルール／アイゼンハワー／WIP 制限 3／エイジング 14 日（本人）・30 日（依頼者）。工数見積の精緻化・主観優先度・RACI は採らない
- D-1〜D-6 は architect 推奨どおり：定型の自動起票は PC-11 で第 1 段階から／四半期・数字は出すが評価は人・個人別は本人と直属上長のみ／閾値 14/30 日は設定値／全員が振れる・匿名不可・受け側は WIP 上限と相談メモで守る／PC-01 追記は別 PR／名称「頼まれ事・放置業務の追跡」

## 11. リポジトリ構成 v2（`2026-09-07-repo-layout-v2.md`）の PM 判断 — 2026-09-07
- 4 区分（①デモ＝`mock/`／②実装＝`dify/`・`scripts/dify`・`tools`／③＝`docs/dify/usecases`・`docs/handoff`・`mock/js/data/scenarios`＋`docs/service-map.md`／④＝`data/world/`・`dify/kb`・`dify/tests`）。マスタ（Cloud）→ 社内・顧客 A・顧客 B へ `dify/env/<env>/env.yml` で差し替えてリリース
- Q1〜Q10 は architect 推奨どおり：`mock/` 改名しない／render は (b) 実値＋置換表／`data/world` 8 ファイル・初版は既存資産から抽出・CI に入れない／タグ `release/<env>/<YYYYMMDD>`／顧客は Community 1.15.x／索引鮮度は verify FAIL／`dify/CHANGELOG.md`／顧客ブランチは切らない／`/dify-deploy` 配置済み
- **Q6：会社・本社の英語表記＝`Seirei Seiko Co., Ltd.`／`Japan HQ`**（PM 確定）
- 実施順：P2 PR-C → v2 PR-1（地図・索引・`data/world`）→ PR-2（`dify/env`＋`render`）→ PR-3（`release`＋CHANGELOG）。`CLAUDE.md` §2-12「環境差分は env に閉じる」は PR-2 と同時に PM が追記
