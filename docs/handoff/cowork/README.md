# docs/handoff/cowork/ — Cowork からの手渡しメモ置き場

Cowork で行った変更（台本・画面案・docs・架空データ）には、必ずここに `YYYY-MM-DD-<slug>.md` の手渡しメモを添える（形式は `COWORK.md` §6）。
クラウドの Claude Code は `/cowork-intake <branch>` でこのメモを起点に取り込む。メモの無い `cowork/` ブランチは取り込まない。
新サービス候補はメモの「提案」欄に書き、採番は `/usecase` が行う（ここでは採番しない）。
