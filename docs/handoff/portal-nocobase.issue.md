# 社内向けポータルサイト（NocoBase）— リポジトリ分離と第 1 段（P0）

設計書: `docs/handoff/2026-09-10-portal-nocobase.md`（**2026-09-11 夕・rev3**）
公式調査の記録: `docs/handoff/2026-09-11-nocobase-research.md`（対象 v2.2.10・出典 URL つき）
レーン: **M/L** ／ ラベル: `run:cloud` ／ Issue: #242 ／ PR: #243（初版）・`bf3ffb3`（rev2）・**本 PR（rev3）**

---

## 🔁 2026-09-11 夕 rev3 —— **PM 判断で 2 件ひっくり返り、画面が 9 → 15 になった**

**忙しい人は設計書の §R-0 と §5-0 だけ読めば分かる。**

### ① NocoBase Professional は**買わない**。**Community（無料）版**で作る

> PM：「**noco プロはまだ買わない。部門内の意識合わせ利用に閉じて使う。**」

**rev2 の推奨（デモも本番も Professional ／ 方式 (b)）は不採用。** rev2 の決定打は「顧客提示デモは Standard の『自社内部利用のみ』を外れうる」だったが、**利用範囲が「部門内の意識合わせ」に狭まったので前提ごと消えた**（判断が誤っていたのではなく材料が変わった）。**経緯は消さずに設計書 §15 附録 A-7 に全文。**

| 失うもの | 帰結 | どこで設計し直したか |
|---|---|---|
| **External PostgreSQL**（Standard+） | **方式 (b) は不可。方式 (a) 一択**（メイン DB 内で Load from database） | §4-11-3'・§3-2 |
| **Migration Manager**（Professional+） | **「同じ定義・別データ」の公式手段が消えた** | **★ §4-12 で自前設計（rev3 でいちばん難しいところ）** |
| Auth: LDAP / OIDC（Professional+） | AD 連携は**本番の話**。買う日まで実装しない | §0-2・§5-8 |
| Version control / Record history / Multi-portal / AI: Knowledge base | **影響なし**（Git・`*_history` で既に代替済み／使わない） | §4-12・§4-13 |
| Workflow: Approval / Webhook / Subflow | Approval は**状態遷移で組む**（§5-20-2）、Webhook は Schedule ポーリングか API キー | §4-13 |

**★ 方式 (a) の歯止め —— 初日に決めて以後変更しない 3 つ**（設計書 §3-5b。**AC-0 で機械検査**）：

| # | 設定 | 初日に決める値 | 後から変えられるか |
|---|---|---|---|
| **G-1** | `DB_UNDERSCORED` | **`true`** | **不可**（DB の作り直し） |
| **G-2** | `DB_TABLE_PREFIX`（システムテーブル側だけ） | **`nb_`** | **不可** |
| **G-3** | 業務テーブルの主キー型 | **`bigint` IDENTITY を Flyway で明示** | 実質不可 |

**⚠️ 本書で「あとで決める」を許さない唯一の場所。** `.env.example` / `docker-compose.yml` / `env/README.md` の **3 か所に同じ値**を書く。

**★ 副産物：P0 のブロッカーから「ライセンス購入待ち」が消えた。今日から着手できる。**

### ② 名刺・顧客データの**正本はポータル**（案 A）。**HubSpot は使わない**

> PM：「**案 A のポータルで。HubSpot は使ってない。**」

- 外部の名刺サービスは**入口だけ**（CSV を一度取り込むだけ。**双方向同期しない**）
- **どのレコードにも `source` / `source_updated_at`**。**下流では編集させない**（編集させるならその時点で `source='portal'` に変える）
- **名刺管理は NocoBase の中**（画像は添付フィールド）。**OCR と言語別の項目抽出は Dify**。**役職は「名刺の原文」と「社内表記」の両方を列で持つ**
- **個人情報の削除依頼に応える経路は 1 本だけ**（§5-12-2）
- **⏳ 越境移転（中国拠点 → 国外 SaaS）は「法務・情シスに確認が要る項目」（L1）として確認待ちのまま進める。** 暫定運用と、返ってきたら変わる場所を対で書いた（§5-12-1）
- **rev2 の案 B（外部サービスを正本）が採られなかった理由は §5-11b に残した**

### ③ メニューが **4 区分・15 画面**になった（rev3 前半の「9 画面」は古い）

| 区分 | 画面（画面 id） |
|---|---|
| — | **ホーム**（`home`） |
| **フロント業務** | 顧客（`cust`）／案件（`proj`）／**ニュース・ウォッチ**（`watch`）／**仕入先・パートナー**（`vend`） |
| **共通業務** | To Do（`act`）／会議（`meet`）／ナレッジ（`know`）／AI サービス（`ai`） |
| **マネジメント** | KPI（`kpi`）／**目標**（`goal`）／要員（`ppl`） |
| **バック業務** | **経費・経理**（`exp`）／**申請・承認**（`req`）／研修・サーベイ（`trn`） |

**足した 6 画面の理由**：モックを PM が触った結果、**「毎日ポータルを開く理由」は案件ではなく To Do・経費・申請・研修**だと分かった。**案件しか無いポータルは、案件を持たない人（PMO・デリバリ）が開く理由が無い。** **「経費・申請は管理部の業務だから外す」は撤回。**

---

## rev3 で新しく設計した中身（設計書の節）

| # | 中身 | 節 |
|---|---|---|
| **★ 定義の移送（Migration Manager 無し）** | **4 層に分けた**：**M0** 運ぶ定義を小さくする（業務スキーマを Flyway に逃がし `collections`/`fields` を再生成できる派生物に格下げ）／**M1** 定義テーブル allowlist の `pg_dump` 定義バンドル（`export-def.sh` / `apply-def.sh --strict` / `manifest.json` で版とG-1〜G-3の一致を強制 / `check-defbundle.mjs`）／**M2** HTTP API の JSON 写し（**diff 監査の正本に昇格**）／**M3** 再現手順書。**自前実装＝NocoBase の保証外であることを明記し、買った日に Migration Manager へ差し替える出口も書いた** | **§4-12** |
| 経費・経理 | 経費精算 ＋ **月次クローズ 6 工程**。`GN-01`（実機・ボタン）＋ `FA-01`〜`FA-05`（構想・カタログ表示）＋ `DC-01`（工程 6）。**止まっている工程が部門の全員に見える**のがこの画面の存在理由 | §5-19 |
| 申請・承認 | 稟議 / 購買 / 契約 / システム利用。**Approval ノード（Professional+）を使わず `requests.state` ＋ `request_steps.decision` で組む**。「いま止まっているところ」＝ `pending` の最小 `seq` | §5-20 |
| ニュース・ウォッチ | `RS-01`〜`RS-04` ＋ `PT-02`/`PT-03`。**AI が「効く先」（どの顧客・案件・誰）を候補として付け、人が確かめる**。**自動で To Do にしない**（3 条件そろったときだけ） | §5-21 |
| 仕入先・パートナー | **`PT-01` はここが正**（顧客画面から外した）。**顧客の与信は営業が見るが、協力会社の与信は PM が見る**——見る人も効く先も違う | §5-22 |
| **目標（MBO）** | 個人 KPI **8 観点**（P0 共通必須 ＋ P1 業績〜P7 業務改善・AI 活用）。**役割別に選べる指標の台帳（`goal_catalog`）**・**測り方（自動/手動）**・**4/20 設定締切・9/30 中間レビュー**・**社員は自分のみ／組織長は配下一覧／PMO は集計ビューのみ**・**未設定と未提出は To Do 自動起票** | §5-23 |
| 個人別 KPI 達成 | **要員画面に勤怠とは別ブロック。本人の行動で動く 5 指標だけ。閲覧は本人と上長** | §5-15-4 |
| サーベイと研修 | **1 本の一覧（区分で絞る）。サーベイ未回答も To Do**。**匿名でも「提出した事実だけ」は別テーブルで持つ**（回答内容と結合できないので匿名は破れない） | §5-16-7 |
| 接触履歴 | **会議・訪問・メール・To Do から自動で積む。人が書き足さない。最終接触は `max(occurred_on)` で出す**（列に持たない） | §5-14-4 |
| To Do 候補 | **3 状態（候補 / 採用 / 見送り）。見送りは消さない**（再提案を止める冪等キー・AI がずれている場所を教える・後から拾える） | §5-4'b |
| ナレッジの操作 | **全社＝「修正を依頼する」のみ／部門＝新規作成・修正・統廃合を提案。権限がある人にだけ描く**（グレーアウトではなく無い）。**統廃合は `retired` ＋ `merged_into_doc_id` でリンクを残して畳む** | §5-17-7 |
| 言語とテーマ | **言語＝プルダウン、テーマ＝アイコン、どちらもヘッダー（プロダクト機能）。本文の翻訳は v1 ではやらない**（ナビ・ラベル・ドロワーのみ 3 言語。台帳の語だけ例外） | §5-9-0 |
| 新しい不明 | **U22**（(a) で取り込んだ既存テーブルに NocoBase が列を足すか）・**U23**（`DB_TABLE_PREFIX` の適用範囲）・**U24**（添付の中間テーブル）・**U25**（定義テーブルの公式一覧が無い）。**R-10 / R-11 で P0 の最初の 1 週に確かめる** | §4-4d |

**⚠️ 消えた不明も 3 つある**（方式 (a) の数少ない利点）：**U11**（外部データソースの接続情報の変数参照）・**U15**（チャート × 外部データソース。**代替 D が丸ごと不要になった**）・**U16**（Own records × 外部テーブル）。

---

## 固有名詞が確定した（**「N 社」は使わない**）

| | 確定 |
|---|---|
| **自社** | **翠雲システムズ / 翠云系统 / Suiun Systems, Ltd.** |
| 自部門 | 中国拠点（上海）ソリューション本部 |
| **自部門 5 名** | **篠崎 悠真**（PM・製造 / `pm`）／**黄 思涵**（PM・金融 / `management`）／**蔡 文博**（デリバリ / `delivery`）／**村井 拓也**（営業 / `sales`）／**岸本 奈津**（PMO / `general`）。**5 名で C3 の 5 ロールを 1 人ずつ埋める** |
| 顧客 | **青嶺精工**（mfg）・**碧洋銀行**（fin）・**α 社・β 社**（仮置き） |
| 協力会社 | **γ 社・δ 社・ε 社**（仮置き） |

**★ 既存 31 名との照合済み：5 名とも衝突なし**（rev2 案の高橋 亮は完全一致、石田 真希は姓が衝突していた。**その照合結果も §14-8'b に残した**）。**ギリシャ文字を選んだのは、既存記号（`K/S/T/…` と `甲〜戊・己`）と見た目で区別が付くため。**

**残る確認は 1 点だけ：「翠雲システムズ」が実在企業と一致しないことを PM が確認する**（public リポなので必ず。`CLAUDE.md` §2-10）。

---

## 並行している 3 つの設計との関係（**設計書 §R-0c / §1-5 / §16 / §17**）

| 相手 | 本書での処理 |
|---|---|
| **`2026-09-11-repo-layout-v3.md`**（NocoBase 実装を dify の `portal/` に取り込む案。**PM 判断待ち**） | **§1（分けた理由）はそのまま残し、§1-5 を足して「v3 で再検討中。結論は v3 に従う」と明記。** `portal` リポの **PR #1 の扱いも v3 に委ねる**。**申し送り「rev2 §4-11 の Professional＋方式 (b) は成り立たない」は R-0【1】で既に処理済み＝整合を確認した**（方式 (a) の歯止め C2 も §3-5 に既存、rev3 で §3-5b に抜き出した） |
| **`2026-09-11-portal-mock-pages.md`**（`mock/portal.html`） | **§16 で役割分担を明示：モック＝見せるもの・UI の正本／本書＝動かすもの・業務ルールの正本。** 食い違ったときどちらが勝つかを事項ごとに表にした（画面 id はモック、文言は本書、`place` はモックのデータ層、テーブル定義は本書） |
| **`2026-09-11-it-industry.md`**（カタログに IT 業を足す。新分類 `sl`・14 分類 77 サービス） | **ポータル発のユースケース 10 件はそちらに吸収された。** → **`docs/handoff/portal-usecases.issue.md` は作らない。§17 を「it-industry を参照する」節に差し替えた**（10 件と本書の出どころの対応表つき）。**管理番号は本書で先に切らない** |

**★ 本書は他の 3 冊を 1 文字も書き換えていない。**

---

## やること（本 Issue の範囲＝現リポのみ）

| PR | 内容 | 主なファイル | 状態 |
|---|---|---|---|
| **PR-1** | 設計書の初版 | `docs/handoff/2026-09-10-portal-nocobase.md` | **マージ済み**（#243） |
| **PR-1b** | 改訂（前提変更＋公式調査） | 同上 ＋ `2026-09-11-nocobase-research.md` | **マージ済み**（`bf3ffb3`） |
| **PR-1c**（**本 PR**） | **rev3 の再改訂 ＋ 本 Issue 本文の更新。コード変更なし** | `docs/handoff/2026-09-10-portal-nocobase.md`・`docs/handoff/portal-nocobase.issue.md` | **本 PR** |
| **PR-2** | **`data/world/dept/` を新設**（**12 ファイル**。§14-8'）＋ `data/world/README.md` に追記 | `data/world/dept/*`・`data/world/README.md` | PR-1c の後。**11-16 を待つのは `knowledge.csv` と `kpi.csv` の 2 本だけ** |
| **PR-3** | `docs/dify/**` の追記（§9-2）＋ `README.md` 1 行 ＋ `CLAUDE.md` 1 行 | `docs/dify/platform-components.md`・`README.md`・`CLAUDE.md` | PR-1c の後。**`CLAUDE.md` の行は構成 v3 の結論待ちにしてよい** |
| **PR-4**（**別 Issue**） | 実機 12 本の Dify アプリ id と公開 Web アプリ URL の登録（P0 のブロッカー B-2） | `dify/env/cloud-master/env.yml`・`mock/js/data/live.js` | **`run:mac` / `run:runner`。本 Issue に含めない** |

### 並列可否

```
PR-1c（本 PR・先行必須）
  ├── PR-2   data/world/dept/**               ┐ 並列可
  └── PR-3   docs/dify/** README.md CLAUDE.md ┘

PR-4 は別 Issue（run:* の軸が違う）
別 Issue（他 architect）: portal-mock-pages（mock/**・tools/**）／it-industry（mock/js/data/**・data/world/it/**）
```

**⚠️ PR-2 と it-industry の PR-1 が `data/world/README.md` で衝突しうる**（両方が表に 1 節足す）。**直列にするか、足す位置を分ける**（`dept/` は末尾、`it/` はその手前）。

## 触らない範囲（reviewer の diff 監査の基準）

- **`mock/**`** — 1 バイトも変えない。データ層（`CATS`/`SVCS`/`TAGS`/`SCENARIOS`/`HOME`/`FEED`/`LIVE`）も不変 → **`tools/regress.mjs --update` は不要**
- **`tools/**`・`tools/regress.baseline.json`・`scripts/**`**
- **`dify/**`**（`apps`・`env`・`kb`・`tests`・`samples`・`state`・`results`）
- **`data/world/**`**（PR-2 まで）
- **`.github/workflows/**`** — 特に `pages.yml` の `path: mock`
- **`.claude/**`・`CLAUDE.md`** — 本 PR では変更しない。§9-1 の 1 行追加は**提案**であり PR-3
- **`docs/dify/**` の既存記述** — PR-3 まで変更しない
- **他 architect の設計書** — `2026-09-11-it-industry.md` / `it-industry.issue.md` / `2026-09-11-repo-layout-v3.md` / `2026-09-11-portal-mock-pages.md`（**読むだけ**）
- **`docs/handoff/portal-usecases.issue.md` を作らない**（§17）

**本 PR が触るのは `docs/handoff/2026-09-10-portal-nocobase.md` と `docs/handoff/portal-nocobase.issue.md` の 2 ファイルだけ。**

## 受け入れ条件（PR-1c）

- `node tools/verify.mjs` **PASS** ／ `node tools/regress.mjs` **PASS**（差分ゼロ）
- **`docs/handoff/**` 以外の diff が 0 行**（`mock/**`・`tools/**`・`dify/**`・`data/**`・`CLAUDE.md`・`.claude/**`）
- **設計書の内部参照（§番号）がすべて通る**（`§15 附録 A-1`〜`A-9`・`§4-4d`・`§5-11b`・`§16`・`§17` を含む）
- **改訂で無効になった記述が、削除ではなく「取り消し線 ＋ 改訂印」か §15 附録 A に残っている**（**rev2 の Professional ＋ 方式 (b) の全文は A-7、C1-b の全文は A-8、KPI の誤りは A-9**）
- **実在企業名が 1 つも書かれていない**（`CLAUDE.md` §2-10）
- **調査レポートが「記載なし」と書いたものが、設計書で断定に変わっていない**（U22〜U25 は「不明」のまま）
- **rev3 で足した 6 画面の文言が ja / zh / en の 3 言語そろっている**（`CLAUDE.md` §2-1 の精神。§5-9）

## 受け入れ条件（PR-2）

- 追加するのは `data/world/dept/` の **12 ファイル**と `data/world/README.md` への追記だけ
- **`mfg`（17 名）・`fin`（14 名）の人を増やさない。** 部員 5 名は `dept/` の中だけ。**姓・名・フルネームの 3 段で既存 31 名と照合する**（§14-8'b の教訓）
- **顧客 2 社の社名は `mfg`/`fin` の `company.md` から引く**（新しい社名を作らない）。**α/β/γ/δ/ε は既存記号と重複しない**
- **mfg と fin を互いに混ぜない**（`CLAUDE.md` §2-13）。`dept/` が両方を顧客に持つのは可（片方向参照）
- **`people_ops.csv` は §5-15-1 の物語が成立するように作る**（Red 案件の担当者 1 名が 残業 15h 超 ＋ 年休取得率 30% 未満 ＋ 必須研修の期限超過）
- **`goals.csv` は 1 名を「未設定」、1 名を「中間未提出」にする**（自動起票のデモ）
- `npm run world` の **warn 件数が増えない** ／ `npm test` PASS（regress は差分ゼロ）

## PM 判断（**18 件。⏳ が 6 件。P0 を止めるものは 1 つも無い**。設計書 §11）

| # | 状態 | 論点 | architect の推奨 |
|---|---|---|---|
| 11-1 ✅ | 完了 | V-PM-1（公式ドキュメント調査） | 実施済み（`2026-09-11-nocobase-research.md`） |
| 11-2 ⏸ | 保留 | PC-19 の採番 | P2 へ。**使わない PC 番号を先に切らない** |
| **11-3** ⏳ | 回答待ち | Dify の `user` に社員 ID か仮名か | **仮名。** AD の識別子は GitLab・Outline と共通で、1 つ漏れると横に繋がる |
| 11-4 ✅ | 了承 | NocoBase の AI 機能を使わない | 維持 |
| 11-5 ✅ | 了承（**v3 で再検討中**） | 新リポの名前と可視性 | `shoulang0729/portal`・public。**ただし結論は構成 v3 に従う**（§1-5） |
| 11-6 ✅ | 了承 | §9-1・§9-2 の追記 | 適用（PC-19 だけ保留） |
| 11-7 🔄 | 更新 | `data/world/` に足すもの | **`data/world/dept/` 12 ファイル**（rev3 後半で 5 本増えた） |
| 11-8 ✅ | 了承 | P0 をどこで動かすか | ローカル docker **2 台**（**理由が Migration Manager → M1 定義バンドルに変わった**） |
| **11-9'** ✅ | **PM 回答済み（反転）** | Professional を買うか | ~~買う~~ → **買わない。Community。** 帰結は F-1〜F-9、代替は §4-12・§4-13 |
| **11-10** ⏳ | | 中国拠点から `github.com` に到達できるか | 情シスに確認。否なら GitLab に片方向ミラー。**P1 着手前まででよい** |
| **11-11** ⏳ | （優先度↓） | AD の 5 区分は OU かセキュリティグループか | **Auth: LDAP は Professional+ で買うまで実装しないので急がない** |
| **11-12'** ✅ | **PM 回答済み（反転）** | 勤怠・研修・年休をスコープ外にしてよいか | ~~外す~~ → **スコープに入れる**（要員・研修画面。P1） |
| 11-13 ✅ | 了承（確定） | デモ世界 | **翠雲システムズ／5 名／α β γ δ ε で確定** |
| **11-14** ⏳ | **新規** | `todo_candidates` の `rejected` をいつまで残すか | **行は消さない。一覧には 1 年以内だけ出す** |
| **11-15** ⏳ | **新規** | サーベイ 6 種を匿名にするか記名にするか（**後から変えられない**） | §5-16-4 の推奨どおり。**匿名でも「提出した事実」は別テーブルで持つ**（匿名は破れない） |
| **11-16** ⏳ | **新規・P1 のブロッカー** | ナレッジ中分類 46・KPI 指標 46・目標の指標の**3 言語** | **PM が名前を確定してから 3 言語表にする。** 分類軸はプロダクト判断なので architect が単独で確定させない。**確定まで seed を作らない** |
| **11-17'** ✅ | **PM 回答済み** | 自部門の社名 | **翠雲システムズ / 翠云系统 / Suiun Systems, Ltd.**。**残る確認は「実在企業と一致しないこと」1 点** |
| **11-18'** ✅ | **PM 回答済み** | 自部門 5 名の氏名 | **確定。既存 31 名との衝突なし**（rev2 案の 2 件の衝突は解消） |

## P0 のブロッカー（**1 つ消えた**）

1. ~~NocoBase Professional の購入~~ → **✅ 消滅**（買わない）
2. **実機 12 本の Dify アプリ id と公開 Web アプリ URL の登録**（PR-4・別 Issue）
3. **`data/world/dept/`**（PR-2）

**待つ間にできること**：`migrations/V1__init.sql` ／ **G-1〜G-3 の台帳（AC-0）** ／ **M1 のスクリプト 3 本**。**とくに G-1〜G-3 は初日にやらないと取り返しがつかない。**

## 本 Issue に含めないもの

- **portal 側の実装**（PR-A2 / PR-B / PR-B2 / PR-C / PR-D）。`run:*` の軸が違う
- **PR-4**（Dify の app id / URL 登録。`run:mac` / `run:runner`）
- **`mock/portal.html` の取り込み**（`portal-mock-pages` の Issue）
- **カタログへの IT 業の追加と `SVCS[].place`**（`it-industry` の Issue）
- `npm run export:catalog` / `export:world`（手コピーが 3 回以上ずれたら着手。**構成 v3 が「1 リポ」に決まったら形が変わる**）
